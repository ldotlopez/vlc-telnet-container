name: Publish to GHCR

on:
  push:
    branches: [main]
    # tags:
    #   - "v*-r*"

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      # - name: Extract tag name
      #   id: vars
      #   run: |
      #     set -euo pipefail

      #     FULL_TAG="${GITHUB_REF#refs/tags/}"
      #     if [[ ! "${FULL_TAG}" =~ ^v[0-9]+(\.[0-9]+)+-r[0-9]{3}$ ]]; then
      #       echo "Invalid release tag '${FULL_TAG}'. Expected v<Jellyfin version>-r<project revision>." >&2
      #       exit 1
      #     fi

      #     JELLYFIN_TAG="${FULL_TAG%-*}"

      #     echo "repo_tag=${FULL_TAG}" >> "$GITHUB_OUTPUT"
      #     echo "jellyfin_tag=${JELLYFIN_TAG}" >> "$GITHUB_OUTPUT"

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          context: vlc-telnet/build
          platforms: "linux/arm64,linux/amd64"
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/vlc-telnet:latest
          # ghcr.io/${{ github.repository_owner }}/vlc-telnet:${{ steps.vars.outputs.repo_tag }}
